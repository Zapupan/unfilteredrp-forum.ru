# Как использовать дамп базы данных

## Что такое дамп?

Дамп базы данных - это SQL файл со структурой и данными базы данных.

## Как импортировать дамп в MySQL?

### Способ 1: Через панель управления хостингом

1. Откройте панель управления вашим хостингом
2. Найдите раздел **"Базы данных"** или **"MySQL"**
3. Выберите вашу базу данных: `u3372230_unfilteredrp-bd`
4. Найдите кнопку **"Импорт"** или **"Import"**
5. Выберите файл `database_dump.sql`
6. Нажмите **"Импортировать"** или **"Execute"**

### Способ 2: Через phpMyAdmin (Web интерфейс БД)

1. В панели хостинга нажмите **"Web интерфейс БД"** (или "phpMyAdmin")
2. Выберите базу данных `u3372230_unfilteredrp-bd` слева
3. Перейдите на вкладку **"Импорт"** (Import)
4. Нажмите **"Выберите файл"** (Choose File)
5. Выберите файл `database_dump.sql`
6. Прокрутите вниз и нажмите **"Вперед"** (Go)

### Способ 3: Через командную строку (если есть SSH доступ)

```bash
mysql -u ваш_пользователь -p u3372230_unfilteredrp-bd < database_dump.sql
```

## Что делает этот дамп?

1. ✅ Создает все необходимые таблицы:
   - `users` - пользователи
   - `posts` - посты/темы
   - `comments` - комментарии
   - `messages` - сообщения
   - `favorites` - избранное
   - `reputation` - репутация
   - `notifications` - уведомления
   - `admin_applications` - заявки на админа
   - `roblox_verifications` - верификации Roblox
   - `activity_log` - лог активности

2. ✅ Создает индексы для быстрого поиска
3. ✅ Устанавливает связи между таблицами (FOREIGN KEY)
4. ✅ Создает админа по умолчанию (логин: Admin, пароль: admin123)

## ⚠️ Важные замечания

1. **Пароль админа**: В дампе пароль хранится в открытом виде (`admin123`). 
   В реальном проекте используйте хеширование (bcrypt, например).

2. **Если таблицы уже существуют**: 
   Дамп использует `CREATE TABLE IF NOT EXISTS`, поэтому существующие таблицы не будут удалены.
   Но если хотите начать с чистого листа, сначала удалите все таблицы.

3. **Кодировка**: 
   Используется `utf8mb4_unicode_ci` для поддержки эмодзи и всех символов.

## Что дальше?

После импорта дампа:

1. ✅ База данных готова к использованию
2. ✅ Можно подключать к вашему Node.js серверу
3. ✅ Нужно обновить `server.js` для работы с MySQL вместо SQLite

## Нужна помощь?

Если нужно:
- Обновить `server.js` для работы с MySQL
- Настроить подключение к базе данных
- Создать API endpoints

Просто скажите! 🚀

